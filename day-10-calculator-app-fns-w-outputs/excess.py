# docstrings
# can create documentation for your custome functions like built in ones do

def format_name(f_name, l_name):
    """Take a first and last name and format it to return the title case version of the name."""

# create a little document bubble as you invoke that function
# a disclaimer
# or can use it as a multiline comment